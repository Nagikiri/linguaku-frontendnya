# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Automatically convert third-party libraries to use AndroidX
android.enableJetifier=true

# Enable AAPT2 for resource processing
android.enableAapt2=true

# Use the new Gradle dependency management
# This is no longer needed for Gradle 7.0+, but kept for compatibility
android.useNewApkCreator=true

# R8/ProGuard optimization
android.enableR8=true
android.enableR8.fullMode=false

# Kotlin code style for this project: "official" or "obsolete"
kotlin.code.style=official

# Enable Kotlin incremental compilation
kotlin.incremental=true

# CRITICAL: Force Kotlin 2.0.21 for KSP compatibility
# This must match the version in build.gradle and settings.gradle
kotlinVersion=2.0.21
org.jetbrains.kotlin.version=2.0.21

# Expo-related properties
expo.jsEngine=hermes

# Use this property to enable support for the new architecture.
# This will allow you to use TurboModules and Fabric renderer.
newArchEnabled=false

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Increase build performance
org.gradle.caching=true
org.gradle.configureondemand=true
org.gradle.daemon=true

# Network timeout settings (useful for slow connections)
systemProp.http.connectionTimeout=60000
systemProp.http.socketTimeout=60000
systemProp.https.connectionTimeout=60000
systemProp.https.socketTimeout=60000
